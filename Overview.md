# Учебен лист за Държавен изпит – Компютърни науки (СУ ФМИ)

## ЧАСТ I. ОСНОВИ НА КОМПЮТЪРНИТЕ НАУКИ

### 1. Множества. Декартово произведение. Релации. Функции.

**Акценти:**
- Аксиоми на ZF (поне: обем, отделяне, степенно множество, индукция/безкрайност).
- Бинарни релации и техните 4 свойства: рефлексивност, симетричност, антисиметричност, транзитивност.
- Релации на еквивалентност ↔ разбиване на множеството на класове.
- Частична наредба (рефлексивна, антисиметрична, транзитивна) vs тотална наредба.
- Диаграма на Хасе – само непосредствените покрития, без транзитивни ребра.
- Топологично сортиране – вграждане на частична в тотална наредба.
- Функции: тотална/частична, инекция, сюрекция, биекция.
- Кардиналност, изброимост.
- Принцип на Дирихле (Pigeonhole).

**Развитие на идеите:**

*Аксиоматизация* – защо ни трябва: парадоксът на Ръсел показва, че „множество от всички множества, които не съдържат себе си" е противоречиво. Аксиомата на отделянето казва, че от вече дадено множество можем да отделим подмножество чрез предикат, но не можем да създаваме множества „от въздуха".

*Релация на еквивалентност и класове* – ключово е твърдението, че R е еквивалентност точно тогава, когато класовете ѝ образуват разбиване (всеки елемент е в точно един клас).

*Топологично сортиране* – дадена е частична наредба (DAG), искаме линейна подредба, в която ако a ≤ b в частичната, то a идва преди b в линейната. Алгоритъмът: намираме минимален елемент, премахваме го, повтаряме.

*Принцип на Дирихле* – ако n+1 предмета се разпределят в n кутии, поне една кутия съдържа поне 2 предмета. Обобщена форма: ако kn+1 в n кутии, някоя има поне k+1.

**Типови задачи:** доказване на равенство на множества (диаграма на Вен или алгебра), проверка на свойство на релация, намиране на класове на еквивалентност, индукция, Дирихле.

---

### 2. Основни комбинаторни принципи и конфигурации. Рекурентни уравнения.

**Акценти:**
- 6-те принципа: Дирихле, биекция, събиране, изваждане, умножение, деление, включване-изключване.
- 4-те основни конфигурации: с/без наредба × с/без повторение.
- Биномни коефициенти и Биномна теорема на Нютон.
- Комбинаторни тъждества чрез двукратно броене.
- Решаване на линейни рекурентни уравнения с константни коефициенти.

**Развитие:**

*Четирите конфигурации:*
- С наредба, без повторение: вариации V(n,k) = n!/(n-k)!
- С наредба, с повторение: n^k
- Без наредба, без повторение: C(n,k) = n!/(k!(n-k)!)
- Без наредба, с повторение: C(n+k-1, k)

*Принцип на двукратното броене* – броим един и същ обект по два начина. Класически пример: C(n,k) = C(n,k-1) + C(n-1,k-1) – броим k-подмножества по два начина според това дали съдържат фиксиран елемент.

*Линейни рекурентни уравнения* – за хомогенно a_n = c_1 a_{n-1} + ... + c_k a_{n-k}: характеристичен полином x^k - c_1 x^{k-1} - ... - c_k. Корени λ_1,...,λ_k дават общо решение a_n = A_1 λ_1^n + ... + A_k λ_k^n. При кратни корени се умножава с полином от n. При нехомогенно се търси частно решение от типа на нехомогенността.

**Типови задачи:** броене с включване-изключване (например уравнения със симпликси на цели числа), решаване на рекурентно уравнение.

---

### 3. Графи. Дървета. Обхождания на графи.

**Акценти:**
- Дефиниции: ориентиран/неориентиран (мулти)граф, път, цикъл, свързаност.
- Дърво: свързан, ацикличен граф; коренево дърво; |V| = |E| + 1.
- Покриващо дърво (spanning tree).
- BFS (обхождане в ширина) – с опашка; DFS (обхождане в дълбочина) – с рекурсия/стек.
- Ойлеров цикъл/път: теорема – съществува ойлеров цикъл в свързан мултиграф ⟺ всички върхове са с четна степен. Ойлеров път – точно два върха с нечетна степен (началото и края).

**Развитие:**

*BFS* – от изходен връх посещаваме всички съседи преди да отидем на следващото ниво. Дава най-къси пътища по брой ребра в невзвешен граф. Сложност O(V+E) при списък на съседство.

*DFS* – навлизаме колкото може по-надълбоко. Полезен за: компоненти на свързаност, ойлерови пътища, топологично сортиране, силно свързани компоненти (Тарян).

*Ойлеров цикъл (доказателство)* – необходимост: всеки път, в който влизаме във връх, и излизаме → четна степен. Достатъчност: индукция – тръгваме от връх и вървим докато можем, връщаме се в стартовия (защото четни степени); ако обходът не покрива всичко, намираме връх по обхода с непокрити ребра и вмъкваме нов обход оттам.

**Типови задачи:** построяване на BFS/DFS дърво, разлагане на множество на ребра в минимален брой пътища без общи ребра (=максимален брой нечетни върхове / 2).

---

### 4. Характеризация на регулярните езици. Теорема на Майхил-Нероуд.

**Акценти:**
- Регулярен език (даден от регулярен израз / краен автомат), детерминиран краен автомат (DFA).
- Теорема на Клини: регулярни езици = езиците на DFA = езиците на NFA.
- Релация α ≈_L β ⟺ α^{-1}(L) = β^{-1}(L), където α^{-1}(L) = {w : αw ∈ L}.
- ≈_L е еквивалентност и **дясно-инвариантна** (α ≈_L β ⟹ αx ≈_L βx).
- Теорема на Майхил-Нероуд: L е регулярен ⟺ ≈_L има краен индекс. Минималният DFA има точно толкова състояния, колкото е индексът.

**Развитие:**

*Идея на доказателството:*
- Ако индексът = n, конструираме DFA със състояния = класовете на ≈_L. Преходи: [α] →ᵃ [αa]. Дясната инвариантност гарантира, че това е добре дефинирано.
- Обратно, ако DFA има n състояния, дефинираме α ~ β ⟺ δ*(q_0,α) = δ*(q_0,β). Това е еквивалентност с краен индекс, и тя „рафинира" ≈_L (или съвпада с нея при минимален автомат).

*Защо е важно:*
- Дава **минимизация** на DFA – обединяваме еквивалентни състояния.
- Дава **техника за доказване на нерегулярност** – ако намерим безкрайно много двойки, които не са в ≈_L, езикът не е регулярен. Пример: L = {a^n b^n : n ≥ 0}. Тук a^i ≢ a^j за i ≠ j (защото a^i b^i ∈ L, но a^j b^i ∉ L).

**Типови задачи:** минимизация на DFA, доказване на нерегулярност чрез ≈_L или чрез лемата за разрастването.

---

### 5. Лема за разрастването за контекстносвободни езици. Незатвореност относно сечение и допълнение.

**Акценти:**
- Контекстносвободна граматика (CFG): G = (V, Σ, P, S), правилата са A → α.
- Дърво на извод; контекстносвободен език (CFL).
- CFL са затворени относно: обединение, конкатенация, итерация на Клини (конструкции).
- **Лема за разрастването за CFL:** за всеки CFL L съществува p, така че за всяка дума w ∈ L с |w| ≥ p, w може да се разпише като w = uvxyz със: |vxy| ≤ p, |vy| ≥ 1, и за всяко i ≥ 0: uv^i x y^i z ∈ L.
- CFL **не са** затворени относно сечение и допълнение.

**Развитие:**

*Идея на лемата:* в дървото на извода за достатъчно дълга дума, поне един път от корена до листо има повторение на нетерминал A. Около това повторение „изрязваме" дървото и можем да го „напомпваме" – повтаряме или премахваме.

*Класически нерегулярен пример:* L = {a^n b^n c^n : n ≥ 1} не е CFL. Прилагаме лемата: ако вземем w = a^p b^p c^p и разделим на uvxyz, vxy има дължина ≤ p, значи не може да съдържа едновременно a, b и c. Напомпване нарушава баланса.

*Незатвореност относно сечение:*
- L_1 = {a^n b^n c^m} – CFL (брояч за a/b).
- L_2 = {a^n b^m c^m} – CFL.
- L_1 ∩ L_2 = {a^n b^n c^n} – не е CFL.

*Незатвореност относно допълнение:* следва от законите на Де Морган – ако CFL бяха затворени относно допълнение и обединение, бяха щели да бъдат затворени и относно сечение.

**Типови задачи:** построяване на CFG за даден език, доказване на не-CFL чрез лемата.

---

### 6. Сортиране чрез сравнения във време O(n lg n).

**Акценти:**
- **Двоична пирамида (binary heap):** пълно двоично дърво, в което всеки родител е ≥ децата (max-heap).
- Heapify(i): O(log n), потапя елемент до правилно място.
- Изграждане на пирамида: наивно O(n log n), бързо чрез Heapify отдолу-нагоре – **O(n)**.
- HEAPSORT: построяваме max-heap, изваждаме максимума на последна позиция и възстановяваме. Общо O(n log n).
- MERGESORT: класически „разделяй и владей". Разделяме на две, рекурсивно сортираме, сливаме. T(n) = 2T(n/2) + O(n) ⟹ O(n log n). **Стабилен**.
- Приложение на MERGESORT: броене на инверсии в масив – модифицираме сливането.

**Развитие:**

*Защо бързото построяване е O(n)?* Сумата ∑ от Heapify операциите на различни нива: ∑_{h=0}^{log n} (n/2^{h+1}) · O(h) = O(n) · ∑ h/2^h = O(n).

*Инверсии* – двойка (i,j), i < j, A[i] > A[j]. По време на сливане, ако вземаме от десния масив елемент x, всички останали в левия масив образуват инверсия с x. Общо O(n log n).

**Бележка от конспекта:** на изпита се пада или HEAPSORT, или MERGESORT.

---

### 7. Минимални покриващи дървета.

**Акценти:**
- Задача: тегловен свързан неориентиран граф – намери дърво с минимална сума на теглата, което свързва всички върхове.
- **Теорема за съгласуваното множество (cut property):** за всеки разрез на графа, най-лекото ребро през разреза е в някое МПД.
- **Алгоритъм на Прим:** растеж от един връх, винаги добавяме най-лекото ребро между „вътрешния" и „външния" свят. Сложност:
  - С масив: O(V²).
  - С двоична пирамида: O(E log V).
  - С пирамида на Фибоначи: O(E + V log V).
- **Алгоритъм на Крускал:** сортираме ребрата, добавяме по ред ако не образуват цикъл. Cycle check с Union-Find. Сложност: O(E log E).
- **Union-Find** (Disjoint Set Union, DSU): операции union и find, с path compression и union by rank – почти O(1) амортизирано (обратна функция на Акерман).

**Развитие на коректност (Крускал):** използваме теоремата за съгласуваното множество. Когато добавим ребро e = (u,v), е защото в текущото състояние u и v са в различни компоненти. Това образува разрез, и e е най-лекото подходящо ребро (защото взимаме ребрата сортирано).

**Бележка:** на изпита се пада или Прим, или Крускал.

---

### 8. Най-къси пътища в тегловни графи.

**Акценти и варианти на задачата:**
- Single-source: най-къси пътища от един връх до всички.
- All-pairs: между всяка двойка.
- Проблем с отрицателни ребра: Дийкстра не работи; отрицателен цикъл прави задачата неопределена.

**Четири алгоритъма:**

1. **Дийкстра** (само неотрицателни тегла). Идея: жадно избираме връх с минимална тентативна дистанция, релаксираме съседите. С масив: O(V²); с пирамида: O((V+E) log V).

2. **DAG SSSP:** топологично сортираме графа, после релаксираме ребрата в този ред. Сложност: **O(V+E)** – по-бързо от Дийкстра, и работи с отрицателни тегла (но изисква ацикличност).

3. **Белман-Форд:** релаксираме всички ребра V-1 пъти. Сложност O(VE). Работи с отрицателни тегла. След V-1 итерации, ако още има релаксация на V-та – има отрицателен цикъл.

4. **Флойд-Уоршал:** all-pairs, O(V³) време, O(V²) памет. Динамично програмиране: dp[k][i][j] = най-къс път от i до j, използващ върхове ≤ k. Преход: dp[k][i][j] = min(dp[k-1][i][j], dp[k-1][i][k] + dp[k-1][k][j]).

**Бележка:** на изпита се пада или (Дийкстра + DAG), или (Белман-Форд + Флойд-Уоршал).

---

## ЧАСТ II. ЯДРО НА КОМПЮТЪРНИТЕ НАУКИ

### 9. Компютърни архитектури – формати на данните. Структура на ЦП.

**Акценти:**
- Концепция на Фон Нойман: запомнена програма.
- Формати на данните:
  - Цели двоични: знакови (two's complement) и без знак.
  - Двоично-десетични (BCD): всеки decimal digit = 4 бита.
  - Плаваща запетая (IEEE 754): знак, експонента, мантиса. Single = 32 бита (1+8+23), double = 64 бита (1+11+52).
  - Знакови данни: ASCII, Unicode (UTF-8/16).
- Централен процесор:
  - **Регистри:** общо предназначение, специални (PC/IP, SP, флагов регистър).
  - **АЛУ:** аритметико-логически операции.
  - **Регистри на флаговете:** Z (zero), C (carry), N (negative), O (overflow), P (parity).
  - **Блокове за управление:** дешифрират инструкциите.
  - **Връзка с паметта:** адресна шина, шина за данни, шина за управление.
- **Конвейерна обработка (pipelining):** разделяне на изпълнението в етапи (fetch, decode, execute, memory, writeback) – едновременно се обработват няколко инструкции в различни етапи.

**Развитие:** IEEE 754 single precision стойност = (-1)^s × 1.M × 2^(E-127), където E е сурово прочетената експонента. Денормални числа за много малки числа, специални стойности за NaN и ±∞.

---

### 10. Структура и йерархия на паметта. Сегментна и странична преадресация. Прекъсвания.

**Акценти:**
- Йерархия: регистри → L1/L2/L3 кеш → основна памет (RAM) → виртуална памет (диск). От бързо/малко към бавно/голямо.
- **Сегментна преадресация:** селектор → дескриптор (база + лимит + права) → линеен адрес.
- **Странична преадресация:** виртуалният адрес = (номер на страница) + (offset). Каталог на страниците → таблица → физическа страница. TLB – кеш на преводите.
- **Стратегии за подмяна:** FIFO, LRU, LFU, оптимална (на Беладй – само за анализ), Clock.
- **Прекъсвания:**
  - Видове: хардуерни (от устройства), софтуерни (трапи), изключения (грешки, page fault).
  - Структура на обработка: запазване на контекст → разпознаване → ISR (interrupt service routine) → връщане.
  - Приоритети – контролер на прекъсванията (PIC, APIC).

**Развитие:**

*Защо ни е виртуална памет?* Изолация между процеси, всеки получава свое адресно пространство; разширяване над физически RAM; защита (read-only страници, не-изпълняеми).

*Page fault* – при достъп до страница, която не е в RAM. ОС я зарежда от диска и евентуално изхвърля друга по избрана стратегия.

---

### 11. Файлова система. Функции, структура и реализация.

**Акценти (Linux):**
- **Файлове и атрибути:** име, размер, owner, права (rwx за user/group/other), времеви маркери.
- **Видове обекти:** обикновени файлове, директории, устройства (block/character), pipes (FIFO), сокети, символни връзки.
- **ext2/ext3:**
  - Inode – структура с метаданни и указатели към блокове (директни, един индирекция, двойна, тройна).
  - Директорията е файл, съдържащ списък (име, inode).
- **Ускоряване:** кеширане на четене/запис, отлагане на записа (write-back), **алгоритъм на асансьора** (elevator/SCAN) – подреждаме I/O заявките по адрес, за да минимизираме движенията на главата.
- **Надеждност:** журнална ФС (журналиране на промените преди писане – ext3, NTFS), RAID:
  - RAID 1: огледало (2 диска – пълно копие).
  - RAID 5: striping + parity, толерира 1 повреден диск.
- **POSIX функции:** `open`, `close`, `read`, `write`, `lseek` – като файлов дескриптор.

**Примерни задачи (от конспекта):** работа с двоични файлове в C – четене на uint32_t, обработка на интервали, сортиране на големи бинарни файлове, прилагане на patch файл.

**Развитие:** при големи файлове (>2GB) – нужно е да се внимава с типовете (`off_t`, `lseek64`). Сортирането на 2GB файл с 8-bit стойности може да се направи с **counting sort** – броим колко пъти се среща всеки от 256 байта, после изписваме.

---

### 12. Управление на процеси и междупроцесни комуникации.

**Акценти:**
- Системни примитиви: `fork()` (създава копие на процеса), `exec()` (зарежда нова програма), `exit()` (завършва), `wait()`/`waitpid()` (синхронизация с дъщерен).
- Права: реален vs ефективен UID/GID; setuid биране.
- Групи процеси, сесия – polezni за shell job control.
- **IPC:**
  - Сигнали – асинхронни известия (SIGINT, SIGTERM, SIGKILL...).
  - Pipes (`|`, `pipe()`) – еднопосочни байтови потоци.
  - Named pipes (FIFO).
  - **System V IPC:** обща памет (`shmget`), семафори (`semget`), съобщения (`msgget`).
  - POSIX варианти: `shm_open`, `sem_open`, `mq_open`.

**Развитие:** `fork()` връща два пъти – веднъж в родителя (PID на детето), веднъж в детето (0). Често се ползва pattern `fork()+exec()` за стартиране на нов процес. Без `wait()` остават zombie процеси.

---

### 13. Компютърни мрежи – OSI, маршрутизация, IPv4/IPv6, TCP, DNS.

**Акценти:**
- **OSI модел** (7 нива): физическо, канално (data link), мрежово, транспортно, сесийно, представително, приложно.
- **TCP/IP модел** (4 нива): мрежови интерфейс, IP, транспортно (TCP/UDP), приложно. Съответствието: OSI 1-2 ↔ TCP/IP мрежови интерфейс; OSI 3 ↔ IP; OSI 4 ↔ TCP/UDP; OSI 5-7 ↔ приложно.
- **Маршрутизация:**
  - Distance-vector (RIP) – всеки рутер пази вектор от дистанции, обменя със съседи. Bellman-Ford по същество.
  - Link-state (OSPF) – всеки рутер знае пълната топология, прилага Дийкстра.
- **IPv4:** 32-битов адрес.
  - Класова: A (0xxx), B (10xx), C (110x).
  - Безкласова (CIDR): a.b.c.d/n – първите n бита са мрежа.
- **IPv6:** 128-битов адрес, без NAT нужда, вграден IPsec, по-проста заглавна структура.
- **TCP – тройно ръкостискане (3-way handshake):**
  1. Клиент → SYN, seq=x.
  2. Сървър → SYN+ACK, seq=y, ack=x+1.
  3. Клиент → ACK, ack=y+1.
- **DNS:** разпределена база за имена. Записи: A (IPv4), AAAA (IPv6), MX (mail), CNAME (alias), NS (name server), PTR (reverse). Резолюция: рекурсивна / итеративна. Кеширане с TTL.

---

### 14. Процедурно програмиране – основни конструкции.

**Акценти:**
- **Структурно програмиране** – трите основни конструкции: последователност, избор (if/switch), цикъл (while/for). Доказателство (Бьом-Якопини), че това е достатъчно.
- Управляващи конструкции – условни (if/else, switch), цикли (while, do-while, for).
- Променливи: локални (стек), глобални (data сегмент), статични.
- Инициализация vs присвояване.
- Функции/процедури:
  - **Предаване по стойност:** копира се стойността.
  - **Предаване по име/референция:** функцията може да променя оригинала.
- Типизация и type checking – статична / динамична.

**Развитие:** в C предаването винаги е по стойност, но може да се предаде указател, което емулира предаване по референция. В C++ има истинско предаване по референция с `&`.

---

### 15. Процедурно програмиране – указатели, масиви, рекурсия.

**Акценти:**
- **Указатели:** съдържат адрес. `&x` – адрес на x; `*p` – съдържанието на адреса.
- **Указателна аритметика:** `p+1` е следващият елемент от типа на p, не следващият байт.
- **Масиви:** в C `arr[i]` ≡ `*(arr+i)`. Многомерни – последователно по редове (row-major).
- **Сортиране и търсене в едномерен масив:** bubble/selection/insertion sort – O(n²); линейно търсене – O(n); двоично търсене в сортиран – O(log n).
- **Символни низове** – в C: null-terminated масиви от char.
- **Рекурсия:**
  - Пряка (функция вика себе си) vs косвена (взаимна).
  - Линейна (един рекурсивен вик) vs разклонена (>1 рекурсивен вик).

**Развитие:** рекурсивно решение винаги може да се преведе в итеративно с явен стек. Опашна рекурсия (tail recursion) може да се оптимизира в цикъл.

---

### 16. ООП. Основни принципи. Класове и обекти. Наследяване и капсулация.

**Акценти:**
- **Абстракция със структури от данни:** клас групира данни + операции върху тях.
- **Конструктори:**
  - По подразбиране.
  - Копи-конструктор: `Foo(const Foo&)`.
  - Move (C++11+).
  - Параметризирани.
- **RAII (Resource Acquisition Is Initialization):** ресурс се захваща в конструктора и освобождава в деструктора – автоматично почистване при стек разрушаване.
- **Методи:** декларация, предаване на параметри, this указател.
- **Наследяване:**
  - Производни и вложени класове.
  - Достъп до унаследени компоненти: public/protected/private наследяване.
- **Капсулация:** скриване на представянето; интерфейс vs реализация.
- **Статични членове:** общи за всички инстанции на класа.

**Развитие:** RAII е една от най-силните идеи в C++. Тя резолва ресурсопритежание чрез детерминирано разрушаване (vs garbage collection). Пример: `std::unique_ptr`, `std::lock_guard`.

---

### 17. ООП. Подтипов и параметричен полиморфизъм. Множествено наследяване.

**Акценти:**
- **Подтипов полиморфизъм:** обект от подклас може да се ползва на мястото на обект от базов клас. Виртуални функции в C++ – извикват се чрез **vtable** (таблица на виртуални функции), динамично свързване по runtime тип.
- **Абстрактни методи / класове:** чисто виртуални функции (`virtual void f() = 0;`). Абстрактен клас не може да се инстанцира.
- **Масиви от обекти vs от указатели:** масив от обекти – статично свързване; масив от указатели към базов клас – истински полиморфизъм.
- **Параметричен полиморфизъм:** шаблони (templates) на функции и класове – генеричност, специализирано към съответния тип в compile-time.
- **Множествено наследяване:** клас наследява от няколко базови класа.
- **Diamond problem** – при наследяване от два класа, които имат общ предтек; решение в C++: `virtual` наследяване.

**Развитие:** виртуални методи носят малък runtime overhead (индирекция през vtable), но са основата на полиморфизма. Шаблоните генерират код по време на компилация – няма runtime cost, но компилацията расте.

---

### 18. Структури от данни – стек, опашка, списък, дърво.

**Бележка:** на изпита се избират **две** от изброените структури.

**Списък (List):**
- С една връзка (singly linked) – всеки възел сочи към следващия.
- С две връзки (doubly linked) – сочи към следващия и предишния.
- Добавяне в началото: O(1); търсене/премахване по стойност: O(n).
- Двусвързаният позволява обхождане в двете посоки за сметка на повече памет.

**Стек (LIFO):**
- Операции: `push`, `pop`, `top`.
- Реализации:
  - Статична (масив с фиксиран размер) – O(1), но ограничен.
  - Динамична (масив с пренареждане) – амортизирано O(1).
  - Свързана – винаги O(1), повече памет.

**Опашка (FIFO):**
- Операции: `enqueue`, `dequeue`, `front`.
- Реализации:
  - Кръгова буферна – O(1) и фиксиран размер.
  - С две връзки или с глава и опашка указатели в свързан списък.

**Дървовидни структури:**
- Коренево дърво – йерархия с корен.
- Двоично коренево дърво – всеки възел има ≤ 2 деца.
- Представяне: с указатели; с масив (за пълни дървета: дете на i е 2i+1, 2i+2).
- **Двоично коренево дърво за търсене (BST):** ляво поддърво < корен < дясно поддърво.
  - Добавяне, премахване, търсене: O(h), където h е височина.
  - Балансирано (AVL, червено-черно): h = O(log n).

---

### 19. Функционално програмиране. Обща характеристика. Функции от по-висок ред.

**Акценти:**
- Характерни особености: имутабилност, без странични ефекти (referential transparency), функциите са граждани от първи клас.
- **Примитивни изрази:** атоми, числа.
- **Средства за комбиниране:** прилагане на функция, ред операции.
- **Средства за абстракция:** дефиниране на именувана функция, ламбда абстракция.
- **Оценяване на израз:**
  - Апликативно (call-by-value, стриктно) – първо аргументите, после функцията. Scheme/ML/повечето езици.
  - Нормално (call-by-name, лениво) – подаваме израза, оценяваме само ако трябва. Haskell.
  - При проста крайна оценка двата подхода дават еднакъв резултат, но при безкрайни структури / нетерминиращи изчисления – се различават.
- **Функции от по-висок ред:** функции, които приемат и/или връщат функции. Класически: `map`, `filter`, `fold`.
- **Анонимни (ламбда) функции:** `(lambda (x) (* x x))` в Scheme; `\x -> x*x` в Haskell.

**Развитие:** ленивото оценяване позволява работа с безкрайни структури (потоци); апликативното е по-предсказуемо за памет/време.

---

### 20. Функционално програмиране. Списъци. Потоци и отложено оценяване.

**Акценти:**
- **Списъци:** в Scheme – `cons`, `car`, `cdr`, `nil`. В Haskell – `[1,2,3]`, `head`, `tail`.
- Основни операции: дължина, обръщане, конкатенация, `nth`.
- **Функции от по-висок ред върху списъци:**
  - `map f xs` – прилага f върху всеки.
  - `filter p xs` – оставя само елементите, за които p е истинно.
  - `fold` (`reduce`) – свежда списък до стойност.
- **Безкрайни списъци / потоци:** при ленива семантика `[1..]` е валиден – генерира при поискване.
- **Отложено оценяване (lazy evaluation):** изразът се представя като „обещание" (thunk), което се изчислява при нужда и резултатът се мемоизира.
- Класически пример: безкраен поток на естествените числа, потокът на Фибоначи: `fibs = 0 : 1 : zipWith (+) fibs (tail fibs)`.

**Развитие:** Ситото на Ератостен с безкраен поток – `primes = sieve [2..] where sieve (p:xs) = p : sieve [x | x <- xs, x mod p /= 0]`.

---

### 21. Синтаксис и семантика на предикатното смятане от първи ред.

**Акценти:**
- **Синтаксис:**
  - Език: предикати, функционални символи, константи, променливи, логически свръзки, квантори ∀ ∃.
  - **Терми:** изградени от константи, променливи и функционални символи.
  - **Формули:** атомарни (P(t_1,...,t_n), t_1 = t_2) и съставни (∧, ∨, →, ¬, ∀x φ, ∃x φ).
  - **Област на действие на квантор:** формулата φ в ∀x φ.
  - **Свободна / свързана променлива.** Свободна – не е под обхвата на свой квантор.
  - **Затворена формула (sentence):** няма свободни променливи.

- **Семантика:**
  - **Структура:** A = (A, I), където A е носител (домейн) и I интерпретация на символите.
  - **Оценка на индивидните променливи:** v: Var → A.
  - **Вярност на формула в структура при оценка** – дефинира се рекурсивно.
  - **Тъждествена вярност:** ⊨ φ при всяка структура.
  - **Предикатна тавтология:** φ е тавтология ⟺ е вярна във всяка структура при всяка оценка.

- **Теорема:** Верността на формула не зависи от стойностите на свободните променливи, които не присъстват в нея.
- **Лема за субституциите:** при замяна на променлива с терм, истинността се запазва коректно.

**Типови задачи:** показване, че дадено свойство е дефинимо в структура; показване на изпълнимост чрез посочване на модел.

---

### 22. Изводимост и компютърно генериране на доказателства.

**Акценти:**
- **Литерал:** атомарна формула или нейното отрицание.
- **Дизюнкт (clause):** дизюнкция от литерали. Празен дизюнкт ⊥ – противоречие.
- **Изпълнимо множество от дизюнкти:** съществува модел/оценка, при която всички са верни.
- **Резолвента** (предикатна, либерална):
  - За дизюнкти C_1 ∨ L и C_2 ∨ ¬L', където L и L' могат да се унифицират чрез σ, резолвентата е (C_1 ∨ C_2)σ.
- **Резолютивен извод:** последователност от дизюнкти, всеки от които е от началното множество или резолвента на предишни.
- **Теореми:**
  - **Коректност:** ако от S се извежда ⊥ чрез резолюция, S е неизпълнимо.
  - **Пълнота:** ако S е неизпълнимо, съществува резолютивен извод на ⊥ от S.

**Развитие – доказателство на коректност:** ако резолвентата е дадена от два дизюнкта, и съществува модел на тях, то моделът удовлетворява и резолвентата. Затова, ако S е изпълнимо, всичко изводимо е изпълнимо; следователно не можем да изведем ⊥.

*Връзка с Пролог:* Пролог използва **SLD-резолюция** (Selective Linear Definite) – ограничен вариант, който е пълен за хорнови клаузи.

---

### 23. Бази от данни. Релационен модел.

**Акценти:**
- **Релационен модел:**
  - **Домейн:** множество от допустими стойности.
  - **Релация:** подмножество на декартово произведение D_1 × ... × D_n.
  - **Кортеж (tuple):** елемент на релацията – ред.
  - **Атрибут:** именувана колона.
  - **Схема на релация:** R(A_1:D_1, ..., A_n:D_n).
  - **Схема на БД:** колекция от схеми на релации.
- **Релационна алгебра – основни операции:**
  - Обединение ∪
  - Разлика −
  - Декартово произведение ×
  - Селекция σ (избиране на редове по условие)
  - Проекция π (избиране на колони)
- **Допълнителни:**
  - Сечение ∩
  - Частно (division) ÷
  - Съединение (theta join) ⋈_θ
  - Естествено съединение (natural join) ⋈ – по общи атрибути

**Типови задачи:** превод между SQL заявка и релационна алгебра; писане на DDL (CREATE) и DML (SELECT, INSERT, UPDATE, DELETE).

---

### 24. Бази от данни. Нормални форми.

**Акценти:**
- **Аномалии:** при вмъкване, премахване, обновяване – нужда от нормализация.
- **Функционални зависимости (FD):** X → Y означава „X определя Y".
- **Аксиоми на Армстронг:**
  1. **Рефлексивност:** ако Y ⊆ X, то X → Y.
  2. **Допълване:** X → Y ⟹ XZ → YZ.
  3. **Транзитивност:** X → Y, Y → Z ⟹ X → Z.
  Изведени правила: обединение, декомпозиция, псевдотранзитивност.

- **Нормални форми:**
  - **1NF:** всички атрибути са атомарни (без списъци, без структури).
  - **2NF:** 1NF + няма частична зависимост на не-ключов атрибут от част от съставен ключ.
  - **3NF:** 2NF + няма транзитивна зависимост на не-ключов атрибут от ключ.
  - **BCNF (Бойс-Код):** за всяка нетривиална FD X → A, X е суперключ.
- **Многозначни зависимости** (X →→ Y): за всяко X, множеството от Y-стойности е независимо от другите атрибути.
- **4NF:** BCNF + няма нетривиални многозначни зависимости освен от суперключ.
- **Съединение без загуба:** декомпозицията R = R_1 ⋈ R_2 е без загуба ⟺ R_1 ∩ R_2 → R_1 или R_1 ∩ R_2 → R_2.

**Типова задача:** дадена схема и FD-та, привеждане към 3NF / BCNF.

---

### 25. Изкуствен интелект. Пространство на състоянията и търсене.

**Акценти:**
- **Пространство на състоянията:**
  - Начално състояние.
  - Действия / преходи.
  - Целево състояние (или предикат).
  - Цена на път (понякога).

- **Локални търсещи алгоритми** (работят с едно текущо състояние, без памет на пътя):
  - **Лъчево търсене (beam search):** пазим k най-добри състояния, разширяваме всички, отново вземаме k най-добри.
  - **Изкачване по хълм (hill climbing):** местим се в съседен възел с по-добра оценка. Засява в локални максимуми, плата, гребени.
  - **Симулирано втвърдяване (simulated annealing):** позволяваме „лоши" ходове с вероятност, която намалява във времето (температура). Избягва локални максимуми.
  - **Генетични алгоритми:** популация от решения; селекция, кросоувър, мутация.

- **Задачи за удовлетворяване на ограничения (CSP):** променливи, домейни, ограничения. Решаване с **търсене с възврат (backtracking)** + heuristics: arc consistency (AC-3), forward checking, namalqavane на конфликти (min-conflicts).

- **Двуигрови игри:**
  - **Мини-макс процедура:** агентът максимизира, противникът минимизира. Рекурсия до листата.
  - **Алфа-бета отсичане:** оптимизация – не разглеждаме клонове, които не могат да повлияят на резултата. Може да намали сложността от O(b^d) до O(b^(d/2)) при добра подредба.

---

### 26. Съвременни софтуерни технологии.

**Акценти:**
- **Софтуерен продукт vs процес.**
- **Фази на софтуерния процес:** анализ на изискванията, дизайн, имплементация, тестване, експлоатация, поддръжка.
- **Модели на софтуерен процес:**
  - **Водопад (waterfall):** последователен – всяка фаза приключва преди следващата. Не толерира промени.
  - **Бърза разработка (RAD):** с CASE инструменти, фокус върху бърза доставка.
  - **Еволюционен:** многократни итерации с нарастваща функционалност.
  - **Прототипен:** изграждаме „груб" прототип за изясняване на изискванията.
  - **Спираловиден (Boehm):** комбинира итерации с явен анализ на риска във всеки цикъл.

- **Гъвкави методи (Agile):**
  - **XP (Extreme Programming):** pair programming, TDD, continuous integration, refactoring, малки релийзи.
  - **SCRUM:** sprints (2-4 седмици), product backlog, sprint backlog, daily standup, retrospective, роли (Product Owner, Scrum Master, Team).

- **Изисквания:**
  - **Функционални:** какво да прави системата.
  - **Нефункционални:** как (производителност, сигурност, надеждност, използваемост).

- **UML:** език за моделиране – класова, последователностна, дейностна, компонентна диаграма.

- **Верификация и валидация:**
  - V&V: „правилно ли строим продукта" (verification) vs „правилния продукт ли строим" (validation).
  - Тестване: unit, integration, system, acceptance.
- **Управление на качеството:** QA процеси, метрики, ревюта.

---

### 27. Архитектури на софтуерни системи.

**Акценти:**
- **Софтуерна архитектура:** структурата на софтуерната система – компоненти, връзки между тях, принципи.
- **Изгледи (views):** различни перспективи – модул (статика), компонент-конектор (динамика), allocation (deployment).
- Защо ни е архитектура: ранно справяне с нефункционални изисквания, комуникация между екипи, последователност на дизайна.

- **Архитектурни стилове:**
  - **Многослоен (layered):** нивата си говорят само с непосредствените съседи. OS-и, мрежови стекове.
  - **MVC (Model-View-Controller):** разделение между данни, представяне, управление. Уеб приложения.
  - **Pipe-and-filter (поточен):** независими филтри, обединени през стандартизирани канали. Unix pipes.
  - **SOA (Service-Oriented Architecture):** разпределени услуги, комуникация чрез добре дефинирани интерфейси (REST, SOAP).
  - Микросервисна – еволюция на SOA.

- **Тактики (architectural tactics):** дизайн решения за конкретни качествени атрибути:
  - За производителност: caching, load balancing, async I/O.
  - За наличност: redundancy, failover, health checks.
  - За сигурност: authentication, authorization, encryption.

- **Процес на проектиране:** идентификация на драйвери → избор на стилове → инстанциране → оценка → итерация.
- **Документиране (Documenting Software Architectures – подходът на SEI):** ползваме изгледи (module, C&C, allocation views), всеки с насочена аудитория и точно дефинирани елементи.

---

## ЧАСТ III. МАТЕМАТИКА И ПРИЛОЖЕНИЯ

### 28. Симетрични оператори в крайномерни евклидови пространства. Диагонализация.

**Акценти:**
- Евклидово пространство V – векторно с положително определено скаларно произведение ⟨·,·⟩.
- Оператор φ: V → V е **симетричен** ⟺ ⟨φ(x), y⟩ = ⟨x, φ(y)⟩ за всички x, y ∈ V.
- В ортонормиран базис матрицата на симетричен оператор е симетрична (A = A^T).

**Основни свойства (с доказателства):**
1. Всички характеристични корени са реални.
   *Идея:* Aλ-уравнението λ⟨v,v⟩ = ⟨Av,v⟩ = ⟨v,Av⟩ = λ̄⟨v,v⟩ ⟹ λ = λ̄.
2. Собствени вектори за различни собствени стойности са ортогонални.
   *Идея:* λ_1⟨v_1,v_2⟩ = ⟨Av_1,v_2⟩ = ⟨v_1,Av_2⟩ = λ_2⟨v_1,v_2⟩, и тъй като λ_1 ≠ λ_2, имаме ⟨v_1,v_2⟩ = 0.
3. **Теорема за диагонализация:** Съществува ортонормиран базис, в който матрицата на симетричен оператор е диагонална.
   *Идея на доказателство:* индукция по размерността; намираме собствен вектор v_1, нормираме, гледаме ограничението на φ върху ортогоналното допълнение {v_1}^⊥ – φ запазва това подпространство, защото е симетричен.

**Развитие:** спектралната теорема означава, че симетричните оператори са „най-добрите" – виждаме ги като скалиране по ортогонални оси. Огромно приложение в PCA, оптимизация (Hessian), физика (квантова механика).

---

### 29. Симетрична и алтернативна група. Теорема на Кейли.

**Акценти:**
- **Симетрична група S_n** – всички биекции на {1,...,n}, операцията е композиция. |S_n| = n!.
- **Представяне на елементите като произведение на независими цикли** – всяка пермутация се разлага по уникален начин на дизюнктни цикли.
- **Спрягане в S_n:** σ τ σ^{-1} има същата циклова структура като τ.
- **Транспозиции:** цикли с дължина 2. Всяка пермутация е произведение на транспозиции. Четността е инвариант.
- **Алтернативна група A_n:** подгрупата на четните пермутации. |A_n| = n!/2.
- **Теорема на Кейли:** Всяка крайна група G е изоморфна на подгрупа на S_|G|.
  *Идея:* за g ∈ G, лявото умножение L_g: x ↦ gx е биекция на G. Преслужва инжективен хомоморфизъм G → S_G.
- **Хомоморфизъм на групи:** f: G → H, f(ab) = f(a)f(b).
  - **Ядро (Ker f):** {g ∈ G : f(g) = e_H} – подгрупа на G, нормална.
  - **Образ (Im f):** f(G) – подгрупа на H.
- **Теорема за хомоморфизмите (Първа теорема за изоморфизмите):** G/Ker f ≅ Im f.

---

### 30. Теорема на Ферма. Теореми за средните стойности. Формула на Тейлър.

**Акценти:**

- **Локален екстремум:** x_0 е локален максимум, ако съществува δ > 0 с f(x) ≤ f(x_0) за |x − x_0| < δ.

- **Теорема на Ферма** (необходимо условие за вътрешен екстремум): Ако f има локален екстремум в x_0 от вътрешността на дефиниционната област и f е диференцируема в x_0, то f'(x_0) = 0.
  *Идея:* ляво и дясно лимит на (f(x)−f(x_0))/(x−x_0) имат противоположни знаци (или нулеви), значи производната е 0.

- **Теорема на Рол:** f непрекъсната в [a,b], диференцируема в (a,b), f(a) = f(b) ⟹ ∃ c ∈ (a,b) с f'(c) = 0.
  *Идея:* f достига max и min по Вайерщрас. Ако и двете са на краищата → f е константа; иначе вътрешен екстремум → Ферма.

- **Теорема на Лагранж** (средна стойност): ∃ c ∈ (a,b): f(b) − f(a) = f'(c)(b − a).
  *Идея:* прилагаме Рол към h(x) = f(x) − (наклонена права през крайните точки).

- **Теорема на Коши** (обобщение): при g'(x) ≠ 0 в (a,b): (f(b)−f(a))/(g(b)−g(a)) = f'(c)/g'(c).
  *Идея:* Рол върху h(x) = (f(b)−f(a))g(x) − (g(b)−g(a))f(x). По пътя – g(a) ≠ g(b) (иначе по Рол g'(c)=0).

- **Формула на Тейлър с остатъчен член във форма на Лагранж:**
  
  f(x) = f(a) + f'(a)(x−a) + ... + f^(n)(a)(x−a)^n/n! + f^(n+1)(c)(x−a)^(n+1)/(n+1)! за някое c между a и x.
  *Идея:* индукция по n + Коши/Рол приложен към подходяща спомагателна функция.

---

### 31. Определен интеграл. Интегруемост на непрекъснатите функции. Нютон-Лайбниц.

**Акценти:**
- **Разбиване на [a,b]:** P: a = x_0 < x_1 < ... < x_n = b.
- **Големи (горни) и малки (долни) суми на Дарбу:**
  - S(f,P) = ∑ M_i (x_i − x_{i-1}), където M_i = sup_{[x_{i-1},x_i]} f.
  - s(f,P) = ∑ m_i (x_i − x_{i-1}), m_i = inf.
- При **уплътняване** на разбиването S не нараства, s не намалява.
- **Римановски интеграл (чрез Дарбу):** f е интегруема ⟺ inf_P S(f,P) = sup_P s(f,P). Тази обща стойност е ∫_a^b f(x)dx.

- **Критерий за интегруемост:** f е интегруема ⟺ за всяко ε > 0 ∃ разбиване P: S − s < ε.

- **Непрекъснатите функции в [a,b] са интегруеми.**
  *Идея:* по теоремата на Кантор f е равномерно непрекъсната, значи ∃ δ: |x−y| < δ ⟹ |f(x)−f(y)| < ε/(b−a). Избираме разбиване с подынтервали < δ, тогава M_i − m_i < ε/(b−a), и S − s < ε.

- **Теорема за средната стойност на интеграла:** ако f е непрекъсната в [a,b], ∃ c ∈ [a,b]: ∫_a^b f(x)dx = f(c)(b−a).
  *Идея:* m(b−a) ≤ ∫f ≤ M(b−a), и f приема всички стойности между m и M (Болцано-Коши).

- **Теорема на Нютон-Лайбниц:** ако f е непрекъсната в [a,b] и F(x) = ∫_a^x f(t)dt, то F'(x) = f(x).
  *Идея:* (F(x+h)−F(x))/h = (1/h)∫_x^{x+h} f(t)dt = f(c_h) за c_h между x и x+h (теорема за средната стойност), а f(c_h) → f(x) при h → 0.

  *Следствие:* ∫_a^b f = G(b) − G(a), където G е примитивна функция на f.

---

### 32. Уравнения на права в равнината.

**Акценти:**
- **Вектoрно-параметрично:** r = r_0 + t·v, t ∈ ℝ; v – направляващ вектор.
- **Координатни параметрични:** x = x_0 + t·a, y = y_0 + t·b.
- **Общо уравнение:** Ax + By + C = 0 (когато A^2 + B^2 ≠ 0). (A,B) е нормален вектор.
- **Декартово (канонично):** (x−x_0)/a = (y−y_0)/b (когато a, b ≠ 0).
- **Нормално уравнение:** x·cos α + y·sin α − p = 0, където α е ъгълът между нормалата и Ox, p е разстоянието от началото до правата.
- **Разстояние от точка M(x_0,y_0) до права Ax+By+C=0:**
  d = |Ax_0 + By_0 + C| / √(A² + B²).
- **Взаимни положения:** успоредни (A_1/A_2 = B_1/B_2 ≠ C_1/C_2), пресичащи се, съвпадащи.
- **Полуравнини:** Ax+By+C > 0 и Ax+By+C < 0.

**Развитие:** ъгълът между две прави с нормали n_1, n_2: cos θ = (n_1·n_2)/(|n_1||n_2|). Перпендикулярност ⟺ n_1·n_2 = 0.

---

### 33. Уравнения на прави и равнина в пространството.

**Акценти:**
- **Векторно-параметрично уравнение на равнина:** r = r_0 + s·u + t·v.
- **Координатно параметрични:** x = x_0 + s·u_1 + t·v_1, аналогично за y, z.
- **Общо уравнение на равнина:** Ax + By + Cz + D = 0; (A,B,C) е нормален вектор.
- **Нормално уравнение:** x·cos α + y·cos β + z·cos γ − p = 0.
- **Разстояние от точка до равнина:** d = |Ax_0 + By_0 + Cz_0 + D| / √(A² + B² + C²).
- **Взаимни положения на две равнини:** успоредни, пресичащи се, съвпадащи.
- **Полупространства.**
- **Права в пространството:**
  - Като сечение на две равнини: { A_1 x + B_1 y + C_1 z + D_1 = 0; A_2 x + B_2 y + C_2 z + D_2 = 0 }.
  - Параметрично: x = x_0 + at, y = y_0 + bt, z = z_0 + ct.
  - Канонично: (x−x_0)/a = (y−y_0)/b = (z−z_0)/c.

---

### 34. Итерационни методи за решаване на нелинейни уравнения.

**Акценти:**
- **Неподвижна точка** на φ: ξ, за която φ(ξ) = ξ.
- **Теорема (Брауер за реалния случай):** ако φ е непрекъснато изображение на [a,b] в [a,b] (т.е. φ([a,b]) ⊆ [a,b]), то φ има поне една неподвижна точка.
  *Идея:* g(x) = φ(x) − x. g(a) ≥ 0, g(b) ≤ 0. По теорема за междинните стойности ∃ ξ с g(ξ) = 0.

- Решаването на f(x) = 0 се свежда до неподвижна точка: x = φ(x) с φ(x) = x − f(x)/(нещо).

- **Свиващо изображение:** φ е свиващо с константа q ∈ [0,1), ако |φ(x) − φ(y)| ≤ q|x − y|.

- **Теорема за неподвижната точка (Банах за ℝ):** ако φ: [a,b] → [a,b] е свиващо с константа q < 1, то:
  - φ има единствена неподвижна точка ξ ∈ [a,b].
  - За всяко x_0 ∈ [a,b] редицата x_{n+1} = φ(x_n) клони към ξ.
  - **Оценка на грешката:** |x_n − ξ| ≤ q^n (b − a) (по-точно: |x_n − ξ| ≤ q^n/(1−q) |x_1 − x_0|).

- **Локална версия:** ако ξ е неподвижна и |φ'(ξ)| < 1, в околност итерациите се сближават с **геометрична прогресия**.

- **Ред на сходимост:** редица {x_n} клони към ξ с ред p, ако |x_{n+1} − ξ| ≤ C |x_n − ξ|^p.
  - Линеен (p = 1): геометрична.
  - Квадратичен (p = 2): броят верни знаци се удвоява.

- **Конкретни методи:**
  - **Метод на хордите (regula falsi):** x_{n+1} = (a·f(x_n) − x_n·f(a)) / (f(x_n) − f(a)). Линеен ред (геометрична).
  - **Метод на секущите:** x_{n+1} = x_n − f(x_n)(x_n − x_{n-1}) / (f(x_n) − f(x_{n-1})). Ред (1+√5)/2 ≈ 1.618.
  - **Метод на Нютон (на допирателните):** x_{n+1} = x_n − f(x_n)/f'(x_n). Квадратичен ред при прост корен.

---

### 35. Дискретни разпределения. Равномерно, биномно, геометрично, Поасоново.

**Бележка:** на изпита комисията дава **две** разпределения.

**Акценти:**
- **Дискретно вероятностно разпределение** на случайна величина X: множество стойности x_1, x_2,... и съответни вероятности p_i = P(X = x_i).
- **Свойства на вероятностите:** p_i ≥ 0 (неотрицателност); ∑ p_i = 1 (нормираност); монотонност на P; адитивност (за дизюнктни събития).

**Четирите разпределения:**

1. **Равномерно дискретно** върху {1,...,n}: P(X = k) = 1/n.
   - E(X) = (n+1)/2.
   - D(X) = (n²−1)/12.
   - *Възниква при:* хвърляне на симетричен зар, случаен избор.

2. **Биномно** Bi(n,p): X = брой успехи в n независими опита с вероятност p всеки.
   - P(X = k) = C(n,k) p^k (1−p)^{n−k}.
   - E(X) = np.
   - D(X) = np(1−p).
   - *Възниква при:* брой герби при n хвърляния, брой дефектни в извадка.

3. **Геометрично** Geo(p): X = брой опит до първи успех (или брой неуспехи преди първи успех – двата варианта се срещат).
   - Вариант „брой опити": P(X = k) = (1−p)^{k−1} p, k = 1,2,...; E(X) = 1/p; D(X) = (1−p)/p².
   - *Възниква при:* колко хвърляния до първи герб, колко опита до първа сделка.

4. **Поасоново** Po(λ): P(X = k) = λ^k e^{−λ}/k!, k = 0,1,2,...
   - E(X) = λ.
   - D(X) = λ.
   - *Възниква при:* брой обаждания в кол център за час, брой клиенти при банков служител, радиоактивни разпадания – събития, които стават „рядко и независимо" с постоянна интензивност.

**Моменти:**
- **Математическо очакване:** E(X) = ∑ x_i p_i (мярка за „централно" положение).
- **Дисперсия:** D(X) = E((X − E(X))²) = E(X²) − E(X)² (мярка за разсейване).

**Пораждаща моментите функция:** M_X(t) = E(e^{tX}). Свойства: M_X(0) = 1; M_X^{(k)}(0) = E(X^k); за независими X, Y: M_{X+Y}(t) = M_X(t)·M_Y(t).

**Защо е удобна:** диференцирането на M в 0 дава моментите – вместо ∑ x^k p_x директно. За биномно: M(t) = (1−p+pe^t)^n; за Поасоново: M(t) = e^{λ(e^t−1)}.

---

